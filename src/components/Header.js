import React  from "react";
import "./Header.css";

const Header = () => {
  return (
    <header className="navbar navbar-expand navbar-dark flex-column flex-md-row top-bar">
      StackTrace
    </header>
  );
};

export default Header;
